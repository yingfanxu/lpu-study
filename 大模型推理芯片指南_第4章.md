# 第 4 章 存储系统设计：推理芯片的真正战场

第 2 章已经定量证明：decode 阶段的性能上限由带宽而非算力决定。因此请建立一个可能违反直觉、但必须刻进脑子的观念：**推理芯片的架构设计中，存储系统才是主角，计算阵列反而是配角**。一颗推理芯片的竞争力，八成写在它的存储层次设计里。

本章自底向上展开：先补齐存储器件的基础知识，再讲片上 SRAM 层次、HBM 集成，最后讲 KV Cache 的硬件管理。

## 4.1 存储器件速成：SRAM、DRAM 与 HBM 到底差在哪

考虑到部分读者主要做逻辑设计、对存储器件谱系不够熟悉，先用最短篇幅把三个主角讲清楚。

**SRAM（静态随机存储）**：6 个晶体管存 1 bit，做在芯片内部，与逻辑同工艺。快（亚纳秒访问）、带宽想要多少设计多少（多 bank 并行），但**密度低、贵**——5nm 级工艺下约 20~30 MB/mm²，一颗 400mm² 的芯片就算一半面积全给 SRAM 也只有几 GB 的零头，装不下大模型权重。

**DRAM（动态随机存储）**：1 个晶体管加 1 个电容存 1 bit，独立工艺制造、独立封装，通过 PCB 或封装基板与主芯片连接。密度高、每 GB 便宜（单条几十 GB 轻松），但访问延迟高（几十上百纳秒），且**带宽受引脚数限制**——普通 DDR5 一个通道 64 bit 宽，一颗芯片能引出的通道有限，总带宽百 GB/s 量级，喂不饱推理负载。

**HBM（High Bandwidth Memory，高带宽存储）**：本质仍是 DRAM，但做了两个关键改造：一是把 8~12 层 DRAM die **垂直堆叠**（用硅通孔 TSV 连接）成一“栈”；二是不走 PCB，而是通过**硅中介层（interposer）**与主芯片并排封装在一起（称 2.5D 封装），中介层上可以布几千根细密导线，单栈接口宽达 1024 bit——是 DDR 通道的 16 倍。HBM3 单栈约 819 GB/s，HBM3E 单栈超 1.2 TB/s，一颗芯片挂 6~8 栈即可达数 TB/s。代价是贵（先进封装 + 堆叠工艺）且容量仍有限（单栈 24~36 GB）。**HBM 就是“用封装技术换带宽”的产物，是当前推理芯片的标配。**

三者构成推理芯片的存储金字塔：SRAM 快而小（片上，MB 级），HBM 是主力仓库（片外近封装，几十 GB、TB/s 级带宽），普通 DDR/LPDDR 作为可选的大容量慢速层。设计的艺术在于让数据在这个金字塔里正确地流动。

## 4.2 带宽规格怎么定：先算账

规格阶段的第一道算术题：目标性能需要多少带宽？公式直接来自第 2 章的负载分析：

```text
每步 decode 访存量 ≈ 权重字节数(batch 内共享,读 1 遍)
                    + 每请求 KV 字节数 × batch

所需带宽 ≈ 每步访存量 ÷ 目标 TPOT ÷ 带宽利用率
```

**算例 4-1：为“70B 模型、batch=32、每用户 30 token/s”定带宽规格。** Llama-70B，INT8 权重 70 GB；GQA 结构（8 个 KV 头、d_head=128、L=80），平均序列 4K，KV 用 FP16：每请求 KV = 2×80×8×128×4096×2B ≈ 1.34 GB，batch=32 共 43 GB。每步总访存 = 70 + 43 = 113 GB。目标每用户 30 token/s 即 TPOT ≤ 33 ms，按 80% 带宽利用率：所需规格带宽 = 113 GB ÷ 33ms ÷ 0.8 ≈ **4.3 TB/s**——约等于 5~6 栈 HBM3，现实中通常由多卡张量并行共同提供。同时核对容量：70+43 = 113 GB，需要相应的多卡 HBM 总容量。

**每一颗推理芯片规格表上的 HBM 栈数，背后都是这样一笔账。**

## 4.3 片上 SRAM：是暂存器，不是缓存

片上 SRAM 在推理芯片中的第一设计原则：**做成软件/编译器显式管理的暂存器（scratchpad），而不是硬件自动管理的缓存（cache）**。理由：CPU 需要 cache 是因为通用程序的访存模式不可预知，只能靠硬件“猜”（标签比对、LRU 替换）；而推理负载的访存模式**编译期就完全已知**——第几拍读哪块权重、写哪块激活，一清二楚。既然全知，cache 的标签存储、比对逻辑、替换机构就是纯浪费，而且 cache 命中与否的不确定性还会破坏执行时间的可预测性。让编译器直接指挥“把权重块 W[3][7] 搬到 scratchpad 地址 0x4000”，又省面积又确定。

**典型三级组织**：PE 本地寄存器（KB 级，服务数据流复用，见 3.5 节）→ 引擎级 scratchpad（数 MB，存当前分块的权重、激活）→ 全芯片共享 SRAM（数十~数百 MB，存层间激活、常驻的权重分块、KV 页缓冲）。

**容量怎么定：双缓冲原理。** SRAM 最小容量由**双缓冲（double buffering，又叫乒乓缓冲）**需求决定。思想很简单：把缓冲区一分为二，计算引擎消费 A 半区的当前分块时，DMA 同时往 B 半区预取下一分块；分块算完，两半角色互换。只要“搬一块的时间 ≤ 算一块的时间”，访存延迟就被完全隐藏，计算引擎永不断粮：

因此 SRAM 至少要装下 2 份工作集分块；分块尺寸又由“掩盖 HBM 延迟与带宽所需的最小计算时长”反推。这一连串推导（目标利用率 → 分块尺寸 → SRAM 容量）应当写进架构文档，而不是拍一个“64MB 差不多”。

**带宽与 bank 划分：比容量更早撞墙的地方。** 工程实践中，SRAM 的**带宽**（端口数 × 位宽 × bank 数）常常比容量更早成为瓶颈：矩阵引擎每拍要吞上百个操作数，向量引擎、注意力引擎、多个 DMA 还要并发读写。单块大 SRAM 只有一两个端口，必然打架；解法是切成许多小 bank（如 32 个），不同引擎经交叉开关访问不同 bank，并通过地址交织（interleaving）把连续访问打散到各 bank。但若两个引擎恰好同拍访问同一 bank，就发生 **bank 冲突**，有人必须等待——冲突率高会实实在在吃掉百分之十几的性能。bank 数、交织粒度、仲裁策略的设计与验证，是 RTL 阶段返工的重灾区，建议在架构阶段就用访存 trace 做冲突率仿真。

## 4.4 HBM 集成要点

芯片侧需要集成 HBM PHY（物理接口，通常外购硬核 IP）和内存控制器。三个工程要点：

**要点一：海岸线（beachfront）是第一平面规划约束。** HBM PHY 必须贴着芯片边缘摆放（离中介层布线最近），6 栈 HBM 的 PHY 基本占满芯片两条长边；而片间互连 SerDes、PCIe 也都要抢边缘。**在架构阶段就要画平面草图**，确认边缘资源够分，否则后期发现放不下就是灾难性返工。

**要点二：带宽利用率是设计出来的。** HBM 标称带宽是理想值，实际能吃到多少取决于访问模式：要凑成长突发（long burst）、顺序访问、避免频繁跨 DRAM 行（row）——每次换行都有激活/预充电开销。好消息是权重读取天然是完美的长顺序流；坏消息是 KV Cache 的分页访问（见 4.5 节）带有随机性。内存控制器要做请求重排、bank 级并行调度，把随机访问尽量整形成对各 bank 的流水访问。设计目标：权重流 >90% 利用率，KV 混合流 >75%。

**要点三：给抖动留余量。** DRAM 定期刷新、高温降频都会造成带宽瞬时波动，性能模型中要预留 5~10% 余量，不要按标称带宽的 100% 做承诺。

## 4.5 KV Cache 的硬件管理：PagedAttention 的硬件视角

第 2 章讲过，多用户服务下 KV Cache 的分配是个动态内存管理问题：请求长短不一、随时到来随时结束，若为每个请求预留最大长度的连续空间，显存利用率可能只有 20~30%。vLLM 的 **PagedAttention** 借鉴操作系统的分页虚拟内存：把每个请求的 KV 切成固定大小的“页”（如每页 16 个 token 的 KV），通过每请求一张“页表”（块表）映射到 HBM 中任意物理位置，彻底消除外部碎片，利用率提到 90% 以上。它已是推理引擎的事实标准，**你的芯片必须为它设计**。

落到注意力引擎的 DMA 上是三条硬性需求：

**(1) Gather 语义。** 注意力计算需要的 KV 页散布在 HBM 各处，DMA 必须能按页表间接寻址：先读页表拿物理页号，再逐页搬运，且页表查找要流水化、可预取，不能每页都停下来等一次查表延迟。

**(2) 变长序列。** 同一 batch 内各请求长度不同（有的 500 token、有的 30000 token），引擎要支持按请求独立的循环边界，决不能按最长请求补零（padding）——那会浪费成倍的带宽和算力。

**(3) 追加写。** 每步 decode 产生的新 K、V 要写到该请求当前页的尾部，页满则从空闲页池取新页并更新页表。这个“追加游标”逻辑放硬件还是固件都可以，但接口语义必须在规格书中定义清楚。

另外，若支持前缀缓存（prefix caching，多个请求共享相同 system prompt 的 KV 页），DMA 还需允许跨请求的只读页共享——硬件上几乎免费，但性能模型要把它带来的带宽节省算进去。

## 4.6 激进路线：SRAM-only 与近存计算

既然 decode 卡在 HBM 带宽，有一条激进路线：**把权重全部装进片上 SRAM，彻底不用 DRAM**（Groq LPU、Cerebras WSE 的选择）。SRAM 聚合带宽轻松做到几十 TB/s（比 HBM 方案高 10~20 倍），decode 速度惊人。代价同样残酷：SRAM 密度低，单芯片只装得下一两百 MB，7B 模型 INT8 也要几十颗芯片串成流水线，系统成本与互连复杂度陡增，且模型越大越吃亏。

另一个研究方向是**近存/存内计算（PIM）**：在 DRAM die 内部嵌入简单运算单元，数据不出 DRAM 就完成粗加工（三星 HBM-PIM、SK 海力士 AiM 已有原型），但生态与编程模型尚不成熟。

主流的务实选择仍是 **HBM + 大片上 SRAM 的混合层次**：用足够的 SRAM 保证权重与 KV 的分块复用，把 HBM 流量压到理论下限——本教程第 8 章的实战设计走的就是这条路。
