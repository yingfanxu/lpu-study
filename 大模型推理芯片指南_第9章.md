# 第 9 章 软硬件协同：编译器与软件栈

“芯片公司死于软件”不是段子：一颗推理芯片的实际可用性 = 硬件能力 × 软件栈成熟度，而后者往往是短板。

最小可行的软件栈自顶向下包括六层。

**前端接图**：导入 PyTorch/ONNX 计算图并做算子合法化。

**图优化**：算子融合（尤其注意力融合与逐元素链融合）、常量折叠、数据布局变换。

**量化工具链**：校准、块缩放因子生成、逐层精度回归报告——必须与算法团队共建，这是自研芯片相对 GPU 的服务质量生命线。

**映射与调度**：分块尺寸选择、双缓冲编排、多引擎流水重叠——这一层直接决定硬件的实际利用率，是编译器的技术核心。

**运行时**：连续批处理调度、分页 KV 管理、多请求抢占。

**算子库与逃生门**：向量单元的 kernel DSL，用于快速支持新算子。

硬件团队必须交付给软件团队三样东西：精确的指令级模拟器（ISS，**先于 RTL 冻结交付**，让编译器开发并行推进）、完善的性能计数器与 profiling 接口、文档化的性能模型。

组织上，让编译器工程师从架构定义第一天就进入项目——映射不友好的硬件特性（奇怪的对齐要求、不规则的 bank 结构）在纸面阶段修改是免费的，流片之后就是要交一辈子的“软件税”。
