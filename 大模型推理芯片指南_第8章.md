# 第 8 章 实战案例：从零设计一颗 7B 模型推理加速器

本章把前七章的方法串成一次完整的设计演练。我们虚构一个贴近真实的项目：设计一颗代号 "Athena-1" 的边缘服务器推理芯片。请以“你就是这个项目的架构师”的心态跟随每一步——所有数字都可以、也应该亲手复算。

## 8.1 需求定义与规格制定

项目输入（来自产品团队）：目标负载为 7B~14B 级稠密模型（Llama/Qwen 类，GQA 结构），主力精度 INT4 权重 + FP8 激活，兜底 BF16；目标场景为企业本地部署的对话与 RAG 服务；性能目标：单芯片、batch=16 时每用户 ≥ 30 token/s(TPOT ≤ 33ms)，2K prompt 的 TTFT ≤ 500ms；最长上下文 32K；整卡功耗 ≤ 150W；工艺 5nm 级；die ≤ 400 mm²；HBM 最多 2 栈（成本敏感，LPDDR5X 备选方案需论证）；配套编译器接收 PyTorch/ONNX 导出图。

架构师的第一项工作，是把这些业务语言翻译成硬件规格——翻译工具就是性能建模。

## 8.2 性能建模：第一步永远是算账

先手算锚点，再写脚本扫描设计空间。

手算锚点（decode，7B，INT4 权重，batch=16，平均序列 4K）：

第一步，每步权重访存：7B 参数 × 0.5 B(INT4)× 1.06（块缩放因子开销）≈ 3.7 GB，batch 内共享、只读一遍。

第二步，每步 KV 访存：GQA 8 个 KV 头、L=32、d_head=128、FP8 KV，每请求每 token 的 KV = 2×32×8×128×1B = 64 KB；序列 4K 时每请求 262 MB，batch=16 共 4.2 GB。

第三步，每步总访存 ≈ 3.7 + 4.2 = 7.9 GB；目标 TPOT 33 ms → 需有效带宽 ≥ 240 GB/s，按 80% 利用率，规格带宽 ≥ 300 GB/s。

结论：单栈 HBM3(819 GB/s)绰绰有余，还留下约 3 倍余量支撑 14B 模型与更长上下文；LPDDR5X 方案需约 5 个 x64 通道才勉强达标且毫无余量——选定 1 栈 HBM3，预留第二栈位置作为大内存 SKU。

容量核对：权重 3.7 GB + KV 最坏情况（32K 上下文、batch16）约 33 GB → 选 24 GB HBM 配置，软件层以分页与配额管理 KV。

第四步，算力规格由 prefill 反推：2K prompt 的 prefill 计算量 ≈ 2 × 7e9 × 2048 ≈ 28.7 TFLOPs(FP8)；TTFT 500ms 中留 400ms 给计算 → 需有效算力 ≥ 72 TFLOPS；按 60% 实际利用率，规格峰值 ≥ 120 TFLOPS(FP8)，对应 INT4 路径约 240 TOPS。

第五步，反查 decode 是否够算力：每步 13.5 GFLOPs × batch 16 ÷ 33ms ≈ 6.5 TFLOPS，远小于 120——再次印证：规格由 prefill 定算力、由 decode 定带宽。

```python
# perf_model.py —— Athena-1 一级性能模型(节选;建议扩展为完整扫参工具)
def decode_step_ms(n_params, bytes_per_w, batch, seq, l, n_kv, d_h,
                   kv_bytes, bw_GBs, eff=0.8, flops_TF=120, mfu_cap=0.6):
    w_bytes      = n_params * bytes_per_w *1.06        # 权重 + 缩放因子
    kv_bytes_tot =2*l*n_kv*d_h*kv_bytes * seq * batch  # KV Cache 读取量
    t_mem = (w_bytes + kv_bytes_tot) / (bw_GBs*1e9*eff) *1e3
    flops =2* n_params * batch                          # 每步近似 FLOPs
    t_cmp = flops / (flops_TF*1e12*mfu_cap) *1e3
    returnmax(t_mem, t_cmp), t_mem, t_cmp               # Roofline:取大者

tpot, tm, tc = decode_step_ms(7e9, 0.5, 16, 4096, 32, 8, 128, 1, 819)
print(f"TPOT={tpot:.1f}ms (访存 {tm:.1f} / 计算 {tc:.2f})")  # ≈12ms,达标
```

把这个模型扩展到：逐层建模（注意力与 FFN 分开）、加入 AllReduce 项（为未来多芯片版预留）、扫描 batch/序列长/量化格式三维空间并输出利用率热力图。这套脚本此后伴随项目全程：它是架构争论的仲裁者，也是流片后 RTL 性能验收的黄金参考。

## 8.3 架构设计与模块划分

关键决策及理由（每一条都能追溯到前面章节的原理）：

矩阵引擎选 4 个 64×64 点积块而非单个 128×128 脉动阵列（3.3/3.4 节）——性能模型显示目标负载中 decode 小 M 维 GEMM 占比高，扫参结果表明 4 块方案在 batch=16 时利用率高约 22%，且 4 个中等块比 1 个巨块更易物理收敛。每块每拍 64×64 次 INT4 乘加，4 块 @1.5 GHz 合计约 245 TOPS，达标。

注意力引擎独立成模块（3.6/4.5 节）——融合 QK 点积、online softmax、加权和三步，内置页表查找与 gather DMA，KV 以 FP8 直接参与计算不做反量化。

SRAM 定 48 MB（4.3 节）——由双缓冲需求推出：最大常驻工作集（FFN 层 gate/up 双矩阵分块 + 激活 + KV 页缓冲）约 18 MB，双缓冲 36 MB，加层间激活与冗余取 48 MB（5nm 下约 110 mm²，占 die 约 1/3）。

控制采用“粗粒度指令+固定流水”（3.7 节风格一）——编译器输出层级指令流，RISC-V 标量核只管队列与异常，首款芯片把控制风险降到最低。

## 8.4 关键模块 RTL 示例：输出驻留点积块

以下给出矩阵引擎中一个 8×8 子块的可综合骨架（真实为 64×64，原理相同），体现输出驻留（OS）数据流：激活按行广播、权重按列广播、部分和驻留在本地累加器中（3.5 节）。

```systemverilog
// os_tile.sv —— 8×8 输出驻留点积块:每拍完成一次 rank-1 更新 C += a·wᵀ
module os_tile #(
  parameter N =8, ACC_W =32
)(
  input  logic clk, rst_n,
  input  logic en, acc_clear,                  // K 维循环起点清累加器
  input  logic signed[7:0] a_row [N],          // 激活:每拍 N 个(行广播)
  input  logic signed[7:0] w_col [N],          // 权重:每拍 N 个(列广播)
  input  logic drain,                          // K 循环结束,逐行排出结果
  output logic signed[ACC_W-1:0] c_out [N],
  output logic c_valid
);
  logic signed[ACC_W-1:0] acc [N][N];

  always_ff @(posedge clk ornegedge rst_n)begin
    if(!rst_n)begin
      for(int i=0;i<N;i++)for(int j=0;j<N;j++) acc[i][j]<= '0;
    endelseif(en)begin
      for(int i=0;i<N;i++)
        for(int j=0;j<N;j++)
          acc[i][j]<=(acc_clear ? '0: acc[i][j])+ a_row[i]*w_col[j];
    endelseif(drain)begin
      // 教学简化:按行移位排出;产品实现应使用独立排出总线与计算重叠
      for(int i=0;i<N-1;i++)
        for(int j=0;j<N;j++) acc[i][j]<= acc[i+1][j];
    end
  end
  assign c_out   = acc[0];
  assign c_valid = drain;
endmodule
```

配套的权重加载路径上串接 5.2 节的块缩放反量化级；排出路径接重量化/舍入级与向量引擎（做偏置、激活、残差）。

必做练习：

(1) 把排出改为与计算重叠的独立端口，推导并验证其对利用率的提升；

（2） 为 a_row 加零值时钟门控，评估功耗收益；

（3） 写出该块利用率公式 U = K/(K + fill + drain)，对 K=128/512/4096 分别求值，与 3.3 节的结论对照。

## 8.5 验证策略

推理加速器验证的特殊性在于数值正确性是一等公民：浮点累加顺序不同导致比特级对齐几乎不可能，必须建立“误差容忍”的黄金模型方法学。

分四层：

(1) 算子级——UVM 环境 + Python 黄金模型（直接调 PyTorch/NumPy，固定随机种子），约束随机生成矩阵形状/精度/边界值（NaN、Inf、饱和、全零），比对采用相对误差与 ULP 双阈值；累加顺序敏感的场景用区间验证。

(2) 指令/层级——编译器输出的指令流在 RTL 仿真与指令级模拟器（ISS）上双跑比对，重点覆盖双缓冲切换、DMA 与计算重叠、分页 gather 等引擎交互；这一层的死锁与握手 bug 是流片风险之首，对所有 valid/ready 接口做形式验证（deadlock/livelock 属性）是性价比最高的投入。

(3) 端到端——FPGA/emulator 上跑通真实 7B 模型（可用缩小配置），与 GPU 参考实现比对 token 序列与 logits 误差；同时采集性能计数器与 8.2 节模型对账，偏差超 10% 必须归因。

(4) 数值鲁棒性专项——量化缩放因子的上下限、长序列 softmax 溢出、KV 页跨界，均为历史事故高发区。

## 8.6 性能评估与迭代闭环

RTL 功能稳定后，用带性能计数器的仿真或 FPGA 原型回归标准负载集（建议矩阵：2K/8K/32K prompt × batch 1/8/16 × 7B/14B），产出三张核心报表：各引擎利用率时间线、HBM 带宽利用率、SRAM bank 冲突率。

将实测与一级性能模型逐项对账。常见“账不平”来源依次为：DMA 与计算重叠不充分（双缓冲颗粒度过大）、SRAM bank 冲突、注意力引擎与矩阵引擎的层内串行化、指令发射间隙。每修一项，回到性能模型补一个修正系数——如此往复，直到模型能以 ±5% 精度预测硬件。

这套“模型—实测”闭环资产比芯片本身更宝贵：它就是下一代芯片规格的起点。
