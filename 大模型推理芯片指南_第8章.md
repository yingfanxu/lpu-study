# 第 8 章 实战案例：从零设计一颗 7B 模型推理加速器

本章把前七章的方法串成一次完整的设计演练。我们虚构一个贴近真实的项目：设计一颗代号 "Athena-1" 的边缘服务器推理芯片。请以“你就是这个项目的架构师”的心态跟随每一步——所有数字都可以、也应该亲手复算。

## 8.1 需求定义与规格制定

项目输入（来自产品团队）：目标负载为 7B~14B 级稠密模型（Llama/Qwen 类，GQA 结构），主力精度 INT4 权重 + FP8 激活，兜底 BF16；目标场景为企业本地部署的对话与 RAG 服务；性能目标：单芯片、batch=16 时每用户 ≥ 30 token/s(TPOT ≤ 33ms)，2K prompt 的 TTFT ≤ 500ms；最长上下文 32K；整卡功耗 ≤ 150W；工艺 5nm 级；die ≤ 400 mm²；HBM 最多 2 栈（成本敏感，LPDDR5X 备选方案需论证）；配套编译器接收 PyTorch/ONNX 导出图。

架构师的第一项工作，是把这些业务语言翻译成硬件规格——翻译工具就是性能建模。

## 8.2 性能建模：第一步永远是算账

先手算锚点，再写脚本扫描设计空间。

手算锚点（decode，7B，INT4 权重，batch=16，平均序列 4K）：

第一步，每步权重访存：7B 参数 × 0.5 B(INT4)× 1.06（块缩放因子开销）≈ 3.7 GB，batch 内共享、只读一遍。

第二步，每步 KV 访存：GQA 8 个 KV 头、L=32、d_head=128、FP8 KV，每请求每 token 的 KV = 2×32×8×128×1B = 64 KB；序列 4K 时每请求 262 MB，batch=16 共 4.2 GB。

第三步，每步总访存 ≈ 3.7 + 4.2 = 7.9 GB；目标 TPOT 33 ms → 需有效带宽 ≥ 240 GB/s，按 80% 利用率，规格带宽 ≥ 300 GB/s。

结论：单栈 HBM3(819 GB/s)绰绰有余，还留下约 3 倍余量支撑 14B 模型与更长上下文；LPDDR5X 方案需约 5 个 x64 通道才勉强达标且毫无余量——选定 1 栈 HBM3，预留第二栈位置作为大内存 SKU。

容量核对：权重 3.7 GB + KV 最坏情况（32K 上下文、batch16）约 33 GB → 选 24 GB HBM 配置，软件层以分页与配额管理 KV。

第四步，算力规格由 prefill 反推：2K prompt 的 prefill 计算量 ≈ 2 × 7e9 × 2048 ≈ 28.7 TFLOPs(FP8)；TTFT 500ms 中留 400ms 给计算 → 需有效算力 ≥ 72 TFLOPS；按 60% 实际利用率，规格峰值 ≥ 120 TFLOPS(FP8)，对应 INT4 路径约 240 TOPS。

第五步，反查 decode 是否够算力：每步 13.5 GFLOPs × batch 16 ÷ 33ms ≈ 6.5 TFLOPS，远小于 120——再次印证：规格由 prefill 定算力、由 decode 定带宽。

```python
# perf_model.py —— Athena-1 一级性能模型(节选;建议扩展为完整扫参工具)
def decode_step_ms(n_params, bytes_per_w, batch, seq, l, n_kv, d_h,
                   kv_bytes, bw_GBs, eff=0.8, flops_TF=120, mfu_cap=0.6):
    w_bytes      = n_params * bytes_per_w *1.06        # 权重 + 缩放因子
    kv_bytes_tot =2*l*n_kv*d_h*kv_bytes * seq * batch  # KV Cache 读取量
    t_mem = (w_bytes + kv_bytes_tot) / (bw_GBs*1e9*eff) *1e3
    flops =2* n_params * batch                          # 每步近似 FLOPs
    t_cmp = flops / (flops_TF*1e12*mfu_cap) *1e3
    returnmax(t_mem, t_cmp), t_mem, t_cmp               # Roofline:取大者

tpot, tm, tc = decode_step_ms(7e9, 0.5, 16, 4096, 32, 8, 128, 1, 819)
print(f"TPOT={tpot:.1f}ms (访存 {tm:.1f} / 计算 {tc:.2f})")  # ≈12ms,达标
```

把这个模型扩展到：逐层建模（注意力与 FFN 分开）、加入 AllReduce 项（为未来多芯片版预留）、扫描 batch/序列长/量化格式三维空间并输出利用率热力图。这套脚本此后伴随项目全程：它是架构争论的仲裁者，也是流片后 RTL 性能验收的黄金参考。
