# 大模型推理芯片总结

## 1. 核心结论

大模型推理芯片不是单纯追求 TOPS 的矩阵计算芯片，而是围绕数据搬运设计的系统。计算阵列决定理论峰值，存储系统、互连和软件栈决定实际性能。

设计时应始终遵循这条链路：

```text
目标模型与服务指标
→ 计算量、权重、激活和 KV Cache 流量
→ 算力、带宽、容量和互连需求
→ 计算阵列、存储层次与数据流
→ 编译器映射、运行时调度和系统验证
```

最重要的方法不是记住某颗芯片的参数，而是能够针对任意“模型 × batch × 序列长度 × 精度”组合快速算账。

## 2. Prefill 与 Decode 是两种不同负载

大模型推理分为两个阶段：

| 阶段 | 主要特征 | 常见瓶颈 | 关键指标 |
|---|---|---|---|
| Prefill | 一次处理整段 prompt，大矩阵乘较多 | 算力 | TTFT |
| Decode | 每步生成一个 token，小 M 矩阵乘并反复读取权重和 KV | 带宽、延迟 | TPOT、token/s |

近似计算量可用：

```text
一次 token 前向计算量 ≈ 2 × 参数量
```

Decode 每步访存量可用：

```text
每步访存量 ≈ 权重字节数 + batch × 每请求 KV Cache 读取量
所需带宽 ≈ 每步访存量 ÷ TPOT ÷ 带宽利用率
```

因此，算力规格通常由 Prefill 目标反推，存储带宽通常由 Decode 目标反推。

## 3. 用 Roofline 判断真正瓶颈

算术强度定义为：

```text
I = 运算量 / 搬运字节数
```

性能上限为：

```text
P = min（计算峰值，存储带宽 × 算术强度）
```

Roofline 不应只用于 HBM，还要分别检查：

- HBM ↔ 全芯片共享 SRAM；
- 共享 SRAM ↔ 引擎级 scratchpad；
- scratchpad ↔ PE 寄存器；
- 芯片 ↔ 芯片互连。

某一级的容量充足并不代表性能达标，端口数、bank 数、位宽、冲突率和访问模式同样重要。

## 4. 计算架构

矩阵引擎的核心目标不是堆乘法器，而是提高操作数复用，降低存储端口压力。

常见选择包括：

- 脉动阵列：结构规整、数据复用高、控制简单，但小矩阵和不规则形状利用率可能较低；
- 点积树或向量化矩阵块：形状适应性较好，更适合 Decode 中的小 M GEMM，但归约和布线更复杂；
- 输出驻留、权重驻留和输入驻留数据流：本质是选择哪类数据留在 PE 内，目标都是减少总搬运量。

推理芯片通常还需要独立的向量单元和注意力引擎，用于 Softmax、归一化、激活、残差以及分页 KV Gather 等操作。

架构设计必须同时考虑平均利用率、尾延迟、物理实现和编译器映射难度。

## 5. 存储系统

典型存储层次为：

```text
PE 寄存器
→ 引擎级 scratchpad
→ 全芯片共享 SRAM
→ HBM
→ 可选 DDR/LPDDR
```

推理负载的数据访问通常可在编译期预测，因此片上 SRAM 更适合设计成由软件或编译器显式管理的 scratchpad，而不是硬件自动替换的 cache。

SRAM 容量应由工作集和双缓冲推导：

```text
计算 tile N 的同时，DMA 加载 tile N+1
隐藏访存的条件：搬一块的时间 ≤ 算一块的时间
```

SRAM 设计还必须检查：

- 每周期供数带宽；
- bank 数与地址交织方式；
- 多引擎并发访问冲突；
- DMA、NoC 和 SRAM 接口宽度是否匹配；
- 背压和队列是否可能形成死锁。

HBM 的标称带宽不等于有效带宽。长突发、DRAM 行命中率、请求重排、刷新和温度降频都会影响实际利用率。

## 6. KV Cache 与注意力

KV Cache 容量随 batch 和序列长度线性增长：

```text
KV 字节数 = 2 × 层数 × KV 头数 × head_dim × 序列长度 × batch × 元素字节数
```

PagedAttention 将每个请求的 KV Cache 切成固定大小的页，通过块表映射到离散物理页，从而减少外部碎片。

硬件需要支持：

- 页表间接寻址和 Gather DMA；
- 查表预取与多请求流水；
- batch 内各请求独立的变长循环边界；
- 新 K、V 的追加写入和物理页分配；
- 前缀缓存的只读页共享；
- KV 的 FP8/INT8 量化路径。

## 7. 精度与量化

常用格式包括 BF16、FP16、FP8、INT8、INT4 和 MXFP。总体趋势是权重、激活和 KV Cache 的位宽持续下降。

基本量化关系为：

```text
x ≈ scale ×（q - zero_point）
```

缩放粒度可分为 per-tensor、per-channel 和 per-group/per-block。粒度越细，精度通常越高，但缩放元数据和硬件处理成本也越高。

混合精度阵列应遵循：

- 累加器显著宽于输入，INT8 通常使用 INT32 累加；
- FP8/FP16 通常使用 FP32 累加；
- 精度模式按 tile 配置，避免逐拍切换；
- 低频格式可走降吞吐复用路径；
- 反量化、舍入、饱和和重量化应有明确的数据通路；
- 使用 round-to-nearest-even 等无偏舍入方式。

## 8. 多芯片互连

三种主要并行方式对互连要求不同：

| 并行方式 | 通信模式 | 主要要求 |
|---|---|---|
| 张量并行 TP | 每层 AllReduce | 极低延迟、高带宽、硬件集合通信卸载 |
| 流水线并行 PP | 层边界激活传输 | 带宽较低，但需要控制流水线气泡 |
| 专家并行 EP | 动态 All-to-All | 拥塞控制、负载均衡和突发缓冲 |

Scale-up 域服务单机或单柜内的强同步通信；Scale-out 域服务跨机柜扩展和 Prefill/Decode 分离后的 KV Cache 迁移。

Chiplet 切分应沿通信最稀疏的边界进行。D2D 带宽虽高，仍不能当作片内连线使用。封装、供电、时钟、复位、DFT、良率和散热必须在架构阶段共同评估。

## 9. 软件栈

最小可行软件栈包括：

1. PyTorch/ONNX 前端接图与算子合法化；
2. 图优化、算子融合和数据布局变换；
3. 量化校准、缩放因子生成和精度回归；
4. 分块、映射、双缓冲及多引擎调度；
5. 连续批处理、分页 KV 和抢占式运行时；
6. 算子库及支持新算子的 Kernel DSL。

硬件团队应在 RTL 冻结前提供指令级模拟器，并提供完整的性能计数器、profiling 接口和可执行性能模型。编译器工程师必须从架构定义阶段参与，否则硬件上的不规则设计会转化为长期的软件成本。

## 10. 验证与工程化

验证应分为四层：

- 算子级：RTL 与 Python/PyTorch 黄金模型比较，覆盖边界值和深累加链；
- 指令级：RTL 与 ISS 双跑，覆盖 DMA、双缓冲和多引擎交互；
- 端到端：FPGA 或仿真平台运行真实模型，比较 logits、token 和性能计数器；
- 数值鲁棒性：覆盖缩放边界、Softmax 溢出、舍入、饱和和 KV 跨页。

物理与流片阶段重点关注：

- 计算阵列层次化布局和时钟树偏斜；
- SRAM bank 与计算单元的物理邻近性；
- HBM PHY 海岸线和 NoC 主干规划；
- Prefill 满载下的功耗、供电与散热；
- SRAM ECC、阵列 BIST、冗余修复和在线巡检；
- 首款芯片优先保证性能模型验证过的主路径。

## 11. 架构设计检查表

在冻结规格前，至少回答以下问题：

- 目标模型、batch、上下文长度和精度是什么？
- TTFT、TPOT、吞吐和尾延迟目标分别是多少？
- Prefill 需要多少有效算力？
- Decode 每步搬运多少权重和 KV，要求多少有效带宽？
- HBM 容量是否容纳权重和最坏情况 KV Cache？
- SRAM 容量是否来自明确的工作集和双缓冲推导？
- SRAM 每周期能否向所有引擎供数，bank 冲突率是多少？
- DMA、NoC 和内存控制器能否维持目标有效带宽？
- 小矩阵、变长序列和不规则算子的阵列利用率是多少？
- TP、PP、EP 或 PD 分离需要怎样的互连？
- 编译器能否表达所有数据搬运、同步和精度模式？
- 是否有 ISS、黄金模型、性能模型和端到端验证计划？
- 面积、功耗、封装、良率和软件开发周期是否同时可接受？

## 12. 最终方法论

推理芯片没有脱离场景的“最佳架构”。TPU、GPU、Groq LPU、Cerebras WSE 和 Trainium 的差异，本质上来自不同的负载假设、商业目标和团队能力。

可靠的设计流程是：

> 先锁定负载与商业目标，定量推导瓶颈，再选择架构赌注并接受其代价，最后用编译器、运行时和验证体系把理论能力转化为稳定的产品性能。

真正值得长期积累的资产不只是某一代 RTL，而是负载分析工具、性能模型、编译器映射能力、验证方法和实测数据库。它们共同决定下一代芯片能否做得更快、更准、更稳。
