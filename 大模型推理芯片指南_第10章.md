# 第 10 章 工程化：物理设计、可靠性与流片要点

本章浓缩推理芯片特有的工程要点（通用 SoC 流程不赘述）。

**物理设计**：计算阵列是高度规整的复制结构，采用层次化布局与硬宏复用，重点管理大面积同步阵列的时钟树偏斜；SRAM 占面积三分之一以上，bank 的摆放决定数据通路绕线长度，应由架构师与后端联合规划；HBM PHY 锁定海岸线后 NoC 主干走向随之确定——**平面规划要在架构阶段就画草图，而不是丢给后端“再说”**。

**功耗**：MAC 阵列的动态功耗占大头，tile 级与 lane 级时钟门控是标配；注意 prefill 才是功耗最坏场景（算力打满），decode 反而“凉快”，150W 目标必须按 prefill 满载做功耗仿真与供电设计。

**DFT 与可靠性**：大阵列天然适合 BIST 加冗余修复（备用行列）；全部 SRAM 上 ECC——KV Cache 里一个位翻转会直接产出乱码 token，用户可感知；数据中心部署还需支持在线巡检（in-field scan）。

**流片节奏**：首款芯片强烈建议“架构做减法”——砍掉多芯片互连（保留接口定义）、砍掉激进的新精度格式、保住性能模型验证过的主路径。一颗按时回片、主路径达标的 A0，远胜纸面完美却延期一年的架构；A0 的实测数据会让 B0 的规格讨论质量脱胎换骨。
