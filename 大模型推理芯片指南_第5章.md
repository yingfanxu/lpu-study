# 第 5 章

## 5.1 数值格式基础：浮点数到底是怎么回事

芯片工程师都懂二进制整数，但浮点格式值得快速复习，因为推理芯片的格式选择直接建立在其结构之上。

一个浮点数由三段组成：**符号位 s、指数段 e、尾数段 m**，数值 = (−1)^s × 1.m × 2^（e−偏置）。两段的位数分配决定了两种能力：**指数位数决定动态范围**（能表示多大和多小的数），**尾数位数决定精度**（相邻两个可表示数之间的间隔）。

理解了这一点，下表的格式谱系就一目了然：

| 格式 | 位分配（符号+指数+尾数） | 特点与典型用途 |
|---|---|---|
| FP32 | 1+8+23 | 全精度基准；推理中仅用于累加器与个别敏感算子 |
| FP16 | 1+5+10 | 通用推理基线、KV Cache 常用；动态范围窄（最大约 65504） |
| BF16 | 1+8+7 | 指数段与 FP32 相同，范围大、精度粗；与 FP32 互转零成本，训练主流 |
| FP8 E4M3 | 1+4+3 | 激活/权重量化，H100 起主流；必须配缩放因子使用 |
| FP8 E5M2 | 1+5+2 | 范围更大精度更粗，多用于训练梯度 |
| INT8 | 8 位整数 | 乘法器最便宜；W8A8 量化方案的载体 |
| INT4 | 4 位整数 | 权重专用量化（GPTQ/AWQ）；计算前常反量化回高精度 |
| MXFP4/6/8 | 每 32 个元素共享一个 8bit 指数 | OCP 开放微缩放标准，新一代硬件（B 系列 GPU）已支持 |

行业趋势十分清晰：**权重位宽一路向下（FP16 → INT8 → INT4 → MXFP4），激活紧随其后（FP8 已普及），连 KV Cache 也在量化（FP8/INT8 KV）**。新芯片规格若不支持 FP8 和 4 位权重路径，竞争力将有硬伤；同时必须保留 BF16 路径，作为精度兜底和新模型快速上线的通道。

## 5.2 量化的原理：一个小例子讲透

**量化（quantization）**就是用低位宽的整数近似表示原始的高精度数值。基本形式：

```text
原值 x ≈ s × (q − z)

其中 q 是低位整数(如 INT4,范围 −8~7),s 是缩放因子(scale)
z 是零点(zero point),对称量化时 z = 0
```

手算一遍就懂了。假设一组权重为 {0.31, −0.52, 0.08, 0.74}，要量化成 INT4（对称量化，z=0）。

第一步，找绝对值最大者 0.74，令它映射到整数最大值 7，得缩放因子 s = 0.74/7 ≈ 0.106。

第二步，每个数除以 s 后四舍五入：0.31/0.106≈3,−0.52/0.106≈−5,0.08/0.106≈1,0.74/0.106=7，量化结果为 {3, −5, 1, 7}，每个只占 4 bit。

第三步（使用时），反量化：3×0.106=0.318,−5×0.106=−0.53……与原值的偏差就是**量化误差**。

存储上，原来 4 个 FP16 共 8 字节，现在 4 个 INT4 加 1 个共享的 s(FP8/FP16)约 3 字节——这就是量化省带宽的全部原理，没有任何魔法。

缩放因子的粒度是量化方案的核心变量：一整个张量共用一个 s（per-tensor，硬件最简单，但一个离群大值会毁掉全组精度）→ 每个输出通道一个 s(per-channel)→ 每 32~128 个元素一组各配一个 s（per-group/per-block，GPTQ、AWQ、MXFP 均采用）。粒度越细，精度越好，但硬件要在数据通路里处理越多的缩放乘法和元数据。

当前的甜点是块级缩放：每 32 个 INT4 权重附带一个 8 bit 共享缩放因子，存储开销仅约 6%，而反量化可以在权重加载路径上顺流水完成，不侵入乘加阵列内部——把“反量化单元”做成权重进入矩阵引擎前的一个独立流水级，是常见且稳健的做法。

离群值（outlier）问题是 LLM 量化的特有难题，必须与算法团队共同决策。LLM 的激活值中，极少数通道会出现比其他值大几十倍的“离群值”；朴素的 INT8 激活量化会让缩放因子被这些离群值撑大，其余正常值全被压扁，精度崩塌。

算法侧有两类对策，分别对应不同的硬件需求：SmoothQuant 类离线变换（把激活的离群幅度数学等价地“迁移”到权重上，推理时无额外操作）——硬件可保持纯 INT8 通路，最省事；混合精度保留（把离群通道单独用 FP16 计算）——硬件需要支持稀疏的高精度旁路，复杂度可观。押哪条路线，必须在架构冻结前与算法团队拍板，并保留可编程后路。

## 5.3 混合精度 MAC 阵列的微架构

一颗芯片要同时支持 BF16/FP8/INT8/INT4，乘加单元怎么做才不至于面积爆炸？核心手法是位级可分解（bit-decomposable）复用：一个 8×8 位乘法器可以拆成 4 个 4×4 子乘法器——INT4 模式下同一块硅每拍完成两倍的乘加吞吐；浮点路径则复用同一批整数乘法器做尾数相乘，外挂指数加法与对齐移位逻辑。

三条铁律：

铁律一：累加器必须显著宽于操作数。 INT8×INT8 的乘积是 16 位；沿 K 维累加 4096 次，最坏情况再涨 log2(4096)=12 位，累加器至少要 28 位，工程上直接用 32 位。FP8/FP16 输入一律用 FP32 累加。累加位宽不足是数值精度事故的第一多发地，而且此类 bug 在小矩阵测试中不会暴露、上了真实模型才炸，务必在验证计划中覆盖最深累加链。

铁律二：精度模式按 tile（分块）粒度切换，不逐拍切换。 模式信号做成准静态配置，大幅简化控制与时序；真实负载中同一层内精度本来就是统一的。

铁律三：为每种格式单独审视综合报告。 低频使用的格式不应付出常驻的关键路径或大面积代价；必要时让冷门格式走“降吞吐复用通路”（如 BF16 以四分之一速率借用 INT8 阵列）而非独立硬件。

## 5.4 实例：支持 INT8/INT4 双模的乘加单元（可综合 RTL）

以下骨架体现上述原则：INT8 模式每拍 1 次乘加，INT4 模式每拍 2 次（高低半字节各一），累加器 32 位。

```systemverilog
// 双模 MAC:mode_int4=0 时做 INT8x8;=1 时把 8bit 拆成两个 4bit 各自乘加
module dual_mode_mac #(
  parameter ACC_W =32
)(
  input  logic              clk, rst_n,
  input  logic              mode_int4,   // 准静态:tile 开始前配置
  input  logic signed[7:0] a,            // 激活(INT4 模式打包两个 4bit)
  input  logic signed[7:0] w,            // 权重(同上)
  input  logic              acc_clear,   // K 维循环起点清累加器
  output logic signed[ACC_W-1:0] acc_out
);
  logic signed[15:0] prod8;
  logic signed[7:0]  prod4_hi, prod4_lo;
  logic signed[16:0] prod_sel;

  assign prod8     = a * w;                              // INT8 全乘
  assign prod4_hi = $signed(a[7:4]) * $signed(w[7:4]);  // INT4 高半
  assign prod4_lo = $signed(a[3:0]) * $signed(w[3:0]);  // INT4 低半
  assign prod_sel = mode_int4 ?(prod4_hi + prod4_lo)    // 子积合并
                              : prod8;

  always_ff @(posedge clk ornegedge rst_n)begin
    if(!rst_n)         acc_out <= '0;
    elseif(acc_clear) acc_out <= prod_sel;
    else                acc_out <= acc_out + prod_sel;
  end
endmodule
```

产品级实现还需在此之外串接：权重路径上的块缩放反量化级（INT4+scale → INT8/FP8）、输出路径上的重量化级（含舍入模式——注意用 round-to-nearest-even，朴素截断会引入系统性偏差，逐层累积后可测量地劣化模型输出）、饱和逻辑、以及 tile 级与 lane 级的时钟门控。

留给读者的练习：

（1） 为该单元补一个带随机激励与黄金模型比对的 testbench，重点打最深累加链与饱和边界；

（2） 综合并对比两种模式下的面积/时序，验证 INT4 路径确实“近乎免费”；

(3) 扩展支持 FP8 E4M3（提示：尾数乘法可复用 prod4 通路，另加指数加法与移位对齐）。
